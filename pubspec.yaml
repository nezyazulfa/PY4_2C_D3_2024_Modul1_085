name: logbook_app_001
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  mongo_dart: ^0.10.8
  google_fonts: ^6.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^7.0.0
  flutter_markdown: ^0.7.7+1
  flutter_svg: ^2.0.10+1  # Untuk gambar SVG
  lottie: ^3.1.0        # Opsional, kalau mau pakai animasi gerak

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  # Registrasi Aset
  assets:
    - assets/Gambar1.png
    - assets/Gambar2.png
    - assets/Gambar3.png
    - .env